version: "3.8"

services:
  plant-disease-api:
    build: .
    container_name: plant-disease-api
    ports:
      - "8000:8000"
    volumes:
      - ./model:/app/model
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
